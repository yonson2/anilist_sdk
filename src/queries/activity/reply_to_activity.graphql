mutation ($activityId: Int, $text: String) {
    SaveActivityReply(activityId: $activityId, text: $text) {
        id
        text
        user {
            id
            name
            avatar {
                medium
            }
        }
        activityId
        createdAt
        likeCount
        isLiked
    }
}
