mutation ($studioId: Int) {
    ToggleFavourite(studioId: $studioId) {
        studios {
            nodes {
                id
                name
                isAnimationStudio
                siteUrl
                favourites
                isFavourite
            }
        }
    }
}