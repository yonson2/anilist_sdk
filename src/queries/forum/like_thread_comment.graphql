mutation ($threadCommentId: Int) {
    ToggleLikeV2(id: $threadCommentId, type: THREAD_COMMENT) {
        ... on ThreadComment {
            id
            isLiked
            likeCount
            user {
                id
                name
            }
        }
    }
}
